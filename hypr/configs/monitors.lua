-- =============================================================================
-- Monitor Configuration
-- =============================================================================

-- Detecta automaticamente resolução nativa e taxa de atualização
hl.monitor({
    output   = "",
    mode     = "highres",
    position = "auto",
    scale    = 1.25,
})
